enum AppPermission {
  camera,
  photos,
  storage,
  locationWhenInUse,
  notification,
}

enum PermissionState {
  granted,
  denied,
  permanentlyDenied,
  restricted,
  limited,
  notSupported,
}

